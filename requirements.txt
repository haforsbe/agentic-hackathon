# Consolidated direct dependencies for the entire workspace
# Direct dependencies only; transitive packages are resolved by pip.
# Versions are locked to the current project virtual environment.

agent-framework-core==1.13.0
agent-framework-openai==1.12.0
azure-ai-projects==2.4.0
azure-identity==1.25.3
mcp==1.26.0
pydantic==2.12.5
python-dotenv==1.2.2
typing_extensions==4.15.0
